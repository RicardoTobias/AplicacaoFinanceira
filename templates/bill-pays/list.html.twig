{% extends 'layout.html.twig' %}

{% block content %}
    <div class="col-md-12 no-padding">
        <div class="panel panel-default">
            <div class="panel-heading">
                <div class="row">
                    <div class="col-xs-6">
                        <h4><i class="fa fa-list" aria-hidden="true"></i> Administração de contas a pagar</h4>
                    </div>
                    <div class="col-xs-6 text-right">
                        <a href="{{ route('bill-pays.new') }}" class="btn btn-primary">
                            <i class="fa fa-plus" aria-hidden="true"></i> Criar novo
                        </a>
                    </div>
                </div>
            </div>
            <div class="panel-body">
                <table class="table table-striped table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>Data Lanc.</th>
                            <th>Nome</th>
                            <th>Valor</th>
                            <th>Categoria</th>
                            <th><center><em class="fa fa-cog"></em></center></th>
                    </tr> 
                    </thead>
                    <tbody>
                        {% for bill in bills %}
                            <tr>
                                <td>{{ bill.date_launch | date('d/m/Y') }}</td>
                                <td>{{ bill.name }}</td>
                                <td>{{ bill.value | number_format(2,',','.') }}</td>
                                <td>{{ bill.categoryCost.name }}</td>
                                <td align="center">
                                    <a class="btn btn-default" data-toggle="tooltip" data-placement="bottom" title="Editar" href="{{ route('bill-pays.edit', {'id': bill.id}) }}">
                                        <span class="fa fa-pencil"></span>
                                    </a>
                                    <a class="btn btn-danger" data-toggle="tooltip" data-placement="bottom" title="Excluir" href="{{ route('bill-pays.show', {'id': bill.id}) }}">
                                        <span class="fa fa-trash"></span>
                                    </a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}